let jwt = require('jsonwebtoken');

/**
 * Check if a user is allowed to get a resource.
 *
 * @param req - request object
 * @param res
 * @param next - callback
 */
function is_authenticated (req, res, next) {

}

module.exports = exports = is_authenticated;